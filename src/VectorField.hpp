//A Field consists of a mathematical object (scalar, vector, tensor,...) defined at every point on a d-dimensional grid. The grid can represent a discretization of either real space or Fourier space.
//For now, we just implement a Vector Field in 3d space (either real or Fourier)

#include <armadillo>

#ifndef VECTOR_FIELD_HPP
#define VECTOR_FIELD_HPP

class VectorField
{
private:

public:


    /*** Methods ***/

    //constructor
    VectorField();

    //destructor
    ~VectorField();
};

#endif